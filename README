creolcomp

Compiles creol programs to the Maude machine.

[ If you do not understand what this means, then this compiler is not
  (yet) for you. ]

Usage:  creolc bla.creol -o bla.maude

To send the result to standard output, use - as a filename, i.e.,

creolc bla.creol -o -

Initial documentation is provided in info.  It may be build using the
rule `make pdf'.  Since this is a very early release it may contain
errors and inaccuracys.

==============================================================================
This small project serves as a prototyping environment for the compiler
and the type checker for Creol.
