/*
 * Deadlock.creol
 *
 * Instances of this class may display a deadlock.
 *
 * main("Deadlock", emp)
 */
class Deadlock
begin
  op init == skip
  op run == var l: Label[ ]; l!n(); release; skip; l?()
  op n == release
end
