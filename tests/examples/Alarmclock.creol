class Alarmclock
 begin
 	var clock: Int

 op init == clock := 0

 with Any
  op wakeme(in n: Int) == var alarmsetting: Int ;
 		alarmsetting := clock + n;
 		await clock >= alarmsetting
  op tick == clock := clock + 1
 end
